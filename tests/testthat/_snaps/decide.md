# the decision-set printout is stable

    Code
      picks
    Output
      Decisions for 12 instances (dflasso)
        12 reached a decision; 0 had no feasible decision.
        Objective sense: maximise value.
      
        A look at two:
          scenario_01  -> 2 of 5 chosen: 1, 2   (predicted total value 0.4)
          scenario_02  -> 2 of 5 chosen: 3, 5   (predicted total value -2.0)
      
        -> decisions(x) for the full action per instance (named by the element ids);
           as_tibble(x) for one tidy row per (instance, element), join-ready.

