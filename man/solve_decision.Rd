% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/solve-decision.R
\name{solve_decision}
\alias{solve_decision}
\alias{solve_support}
\alias{solve_decision,ShortestPathProblem-method}
\alias{solve_support,ShortestPathProblem-method}
\alias{solve_decision,KnapsackProblem-method}
\alias{solve_support,KnapsackProblem-method}
\alias{solve_decision,CapitalAllocationProblem-method}
\alias{solve_support,CapitalAllocationProblem-method}
\alias{solve_decision,FunctionProblem-method}
\alias{solve_support,FunctionProblem-method}
\title{Solve a decision from predicted costs}
\usage{
solve_decision(problem, costs, instance, ...)

solve_support(problem, instance, ...)

\S4method{solve_decision}{ShortestPathProblem}(problem, costs, instance, ...)

\S4method{solve_support}{ShortestPathProblem}(problem, instance, ...)

\S4method{solve_decision}{KnapsackProblem}(problem, costs, instance, ...)

\S4method{solve_support}{KnapsackProblem}(problem, instance, ...)

\S4method{solve_decision}{CapitalAllocationProblem}(problem, costs, instance, ...)

\S4method{solve_support}{CapitalAllocationProblem}(problem, instance, ...)

\S4method{solve_decision}{FunctionProblem}(problem, costs, instance, ...)

\S4method{solve_support}{FunctionProblem}(problem, instance, costs = NULL, ...)
}
\arguments{
\item{problem}{An \linkS4class{OptimizationProblem} object.}

\item{costs}{Numeric vector of predicted costs, one per element row of the
instance.}

\item{instance}{A list of the per-instance data the solver needs. The
built-in problems expect named fields documented on their constructors.}

\item{...}{Reserved for future use.}
}
\value{
\code{solve_decision()} returns the decision vector, the same length and
row order as \code{costs}. \code{solve_support()} returns an integer vector of row
indices into the instance.
}
\description{
The step that turns a vector of predicted costs into a decision for
one instance. \code{solve_decision()} solves each problem its own way: the
built-in problems call their own solvers, and a custom problem calls the
solver supplied to it. \code{solve_support()} reports which element rows a
feasible decision could use for the instance. Those rows set the coverage
rule: a scenario is eligible for the decision-quality step only when every
row in its support has an observed realised cost. The default is all rows,
which can drop scenarios on partially observed data; a custom problem
supplies a narrower set through the \code{solve_support} argument of
\code{\link[=optimization_problem]{optimization_problem()}} when its solver can never use some rows.
}
\details{
For \code{\link[=shortest_path_problem]{shortest_path_problem()}} an unreachable destination raises a classed
condition with class \code{dflasso_infeasible}, so a caller can catch it and mark
the instance infeasible rather than fail the batch.
}
\examples{
problem <- knapsack_problem()
solve_decision(
  problem,
  costs = c(60, 100, 120),
  instance = list(weights = c(10L, 20L, 30L), capacity = 50L)
)
solve_support(problem, instance = list(weights = c(10L, 20L, 30L)))

}
\seealso{
\code{\link[=knapsack_problem]{knapsack_problem()}}, \code{\link[=shortest_path_problem]{shortest_path_problem()}},
\code{\link[=capital_allocation_problem]{capital_allocation_problem()}}, \code{\link[=optimization_problem]{optimization_problem()}}

Other dflasso problems: 
\code{\link{OptimizationProblem-class}},
\code{\link{capital_allocation_problem}()},
\code{\link{knapsack_problem}()},
\code{\link{optimization_problem}()},
\code{\link{sense}()},
\code{\link{shortest_path_problem}()}
}
\concept{dflasso problems}
