% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/data-helpers.R
\name{make_instances}
\alias{make_instances}
\title{Build the per-scenario instances list}
\usage{
make_instances(scenario, ...)
}
\arguments{
\item{scenario}{The per-row scenario vector, the same one passed to
\code{\link[=dfl_fit]{dfl_fit()}}.}

\item{...}{Named arguments placed into each per-scenario entry by their
length.}
}
\value{
A named list with one entry per unique scenario, names aligned to
\code{as.character(unique(scenario))}, ready to pass as \code{instances}.
}
\description{
Turns per-element vectors into the named list of per-scenario instances that
\code{\link[=dfl_fit]{dfl_fit()}} and \code{decide()} expect, with the entry names aligned to the
scenarios.
}
\details{
Each named argument in \code{...} is placed into every scenario entry by length:
\itemize{
\item length equal to \code{length(scenario)}: sliced by scenario, so entry \code{k}
gets the values for the rows in scenario \code{k} (per-element data, such as
item weights).
\item length one: recycled, so every entry gets the same scalar (such as a
shared capacity).
\item length equal to the number of scenarios: placed one per instance, so
entry \code{k} gets the \code{k}-th value (such as one grid size per scenario).
}
List-valued arguments follow the same rules. Slicing is tried before
placement. When the two lengths are equal (one element row per scenario) the
intent is ambiguous, so \code{make_instances()} errors rather than guess: pass a
list for one-per-scenario placement, or wrap a vector in \code{\link[=I]{I()}} to force
per-element slicing.
}
\examples{
scenario <- rep(c("a", "b"), each = 3)
make_instances(scenario, weights = c(2, 4, 1, 3, 5, 2), capacity = 50)
make_instances(scenario, capacity = 50)

}
\seealso{
\code{\link[=dfl_data]{dfl_data()}}, \code{\link[=dfl_fit]{dfl_fit()}}

Other dflasso data helpers: 
\code{\link{dfl_data}()},
\code{\link{simulate_capital_allocation}()},
\code{\link{simulate_knapsack}()},
\code{\link{simulate_shortest_path}()}
}
\concept{dflasso data helpers}
