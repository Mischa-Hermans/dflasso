% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/regret.R
\name{regret}
\alias{regret}
\alias{regret,DecisionFocusedLasso-method}
\alias{print.dfl_regret}
\title{Realised regret of the decision focus against the baseline}
\usage{
regret(
  object,
  x_test,
  cost_test,
  scenario_test,
  instances_test = NULL,
  element_id_test = NULL,
  s = "lambda.min",
  baseline = c("adaptive", "plain", "none"),
  ...
)

\S4method{regret}{DecisionFocusedLasso}(
  object,
  x_test,
  cost_test,
  scenario_test,
  instances_test = NULL,
  element_id_test = NULL,
  s = "lambda.min",
  baseline = c("adaptive", "plain", "none"),
  ...
)

\method{print}{dfl_regret}(x, ...)
}
\arguments{
\item{object}{A \linkS4class{DecisionFocusedLasso} object.}

\item{x_test}{Numeric feature matrix of held-out rows.}

\item{cost_test}{Numeric vector of realised costs for the held-out rows, \code{NA}
allowed for unobserved elements.}

\item{scenario_test}{Vector grouping the held-out rows into instances.}

\item{instances_test}{Optional named list of per-instance data. \code{NULL}
(default) auto-builds it from \code{scenario_test}.}

\item{element_id_test}{Optional per-row element ids. \code{NULL} (default) uses
per-scenario row positions.}

\item{s}{Penalty strength for the predicted costs: \code{"lambda.min"} (default),
\code{"lambda.1se"}, or a numeric lambda.}

\item{baseline}{Which baseline to compare against: \code{"adaptive"} (default, the
prediction-focused adaptive lasso), \code{"plain"} (the plain lasso), or \code{"none"}
for no comparison.}

\item{...}{Unused, present for S4 compatibility.}

\item{x}{A \code{dfl_regret} object.}
}
\value{
An S3 \code{dfl_regret} object with the mean and per-instance regret for
the decision focus and the baseline, the baseline label, and the instance
counts (\code{n_instances}, \code{n_proxy_eligible}, \code{n_partial_coverage},
\code{n_infeasible}). Its \code{print()} shows the head-to-head, the signed percent
change, and the coverage line.
}
\description{
On held-out data, \code{regret()} measures how much worse the decision-focused
fit's decisions turned out than the best possible in hindsight, and does the
same for a prediction-focused baseline on the same instances.
}
\details{
Instances where a solved element has no realised cost are set aside (and
counted), as are infeasible ones. Both approaches are scored on the same
eligible instances.
}
\examples{
sim <- simulate_capital_allocation(60, 6, 6, seed = 1)
fit <- dfl_fit(
  sim$x, sim$cost, sim$scenario,
  problem = capital_allocation_problem(max_weight = 0.5),
  element_id = sim$element_id,
  control = dfl_control(seed = 1, n_splits = 5L)
)
regret(fit, sim$x, sim$cost, sim$scenario)

}
\seealso{
\code{\link[=decide]{decide()}}, \code{\link[=dfl_fit]{dfl_fit()}}.

Other dflasso workflow: 
\code{\link{decide}()},
\code{\link{dfl_fit}()},
\code{\link{dfl_score}()},
\code{\link{predict-coef}}
}
\concept{dflasso workflow}
