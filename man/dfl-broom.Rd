% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/broom-methods.R
\name{dfl-broom}
\alias{dfl-broom}
\alias{tidy.DecisionFocusedLasso}
\alias{glance.DecisionFocusedLasso}
\alias{augment.DecisionFocusedLasso}
\title{Broom methods for a fitted decision-focused lasso}
\usage{
\method{tidy}{DecisionFocusedLasso}(x, s = "lambda.min", ...)

\method{glance}{DecisionFocusedLasso}(x, ...)

\method{augment}{DecisionFocusedLasso}(
  x,
  x_new,
  scenario_new,
  instances_new = NULL,
  element_id_new = NULL,
  newdata = NULL,
  s = "lambda.min",
  ...
)
}
\arguments{
\item{x}{A \linkS4class{DecisionFocusedLasso} object.}

\item{s}{Penalty strength the reported coefficient is read at:
\code{"lambda.min"} (default), \code{"lambda.1se"}, or a numeric lambda.}

\item{...}{Unused, present for method compatibility.}

\item{x_new}{Numeric feature matrix of new rows, the \code{\link[=decide]{decide()}} features.}

\item{scenario_new}{Vector grouping the new rows into instances.}

\item{instances_new}{Optional named list of per-instance data, \code{NULL}
auto-builds it from \code{scenario_new}.}

\item{element_id_new}{Optional per-row element ids, \code{NULL} uses per-scenario
row positions.}

\item{newdata}{Optional data frame to attach the decision columns to, joined
on \verb{(scenario, element_id)}. \code{NULL} attaches them to a fresh element-row
tibble.}
}
\value{
\code{tidy()} returns a tibble with one row per feature and columns
\code{term}, \code{estimate}, \code{proxy_score}, \code{adaptive_weight}, \code{penalty_factor},
\code{role}. \code{glance()} returns a one-row tibble of model-level fields.
\code{augment()} returns the new rows with \code{.predicted_cost} (and the decision
columns when a solver is available) attached.
}
\description{
The broom verbs for a \linkS4class{DecisionFocusedLasso} fit. \code{tidy()} returns one
row per feature with its decision-stage coefficient, its
decision-relevance score, the penalty the decision focus used, and its role.
\code{glance()} returns a one-row model summary. \code{augment()} runs \code{\link[=decide]{decide()}} on
new rows and attaches the predicted cost (and, with a solver, the decision)
under broom's \code{.}-prefix convention. \code{augment()} reads only the features,
never the realised cost.
}
\examples{
sim <- simulate_capital_allocation(60, 6, 6, seed = 1)
fit <- dfl_fit(
  sim$x, sim$cost, sim$scenario,
  problem = capital_allocation_problem(max_weight = 0.5),
  element_id = sim$element_id,
  control = dfl_control(seed = 1, n_splits = 5L)
)
tidy(fit)
glance(fit)
augment(fit, sim$x, sim$scenario, element_id_new = sim$element_id)

}
\seealso{
\code{\link[=dfl_fit]{dfl_fit()}}, \link{summary.DecisionFocusedLasso}, \code{\link[=regret]{regret()}}
}
