% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/simulate.R
\name{simulate_capital_allocation}
\alias{simulate_capital_allocation}
\title{Simulate a capital-allocation problem}
\usage{
simulate_capital_allocation(n_scenarios, n_assets, n_features, seed = NULL)
}
\arguments{
\item{n_scenarios}{Number of scenarios, each one optimisation instance.}

\item{n_assets}{Number of assets per scenario.}

\item{n_features}{Number of feature columns. The first two are the
decision-relevant features, the next few predict the return, and the rest
are noise. Must be at least three.}

\item{seed}{Single integer, or \code{NULL} to draw one at random. The same seed
gives the same data.}
}
\value{
A list with \code{data} (a data frame: \code{feat_01} .. \verb{feat_<n_features>},
\code{realized_return}, a \code{scenario} factor with levels \code{scenario_01} .., and
\code{asset_id}), plus \code{x} (the feature matrix), \code{cost} (the realized returns),
\code{scenario}, and \code{element_id} (the asset ids). The
\code{decision_relevant_features} attribute on \code{data} records the decision
feature names.
}
\description{
Builds an example predict-then-optimize dataset for the capital-allocation
problem. Each scenario is one
realisation of uncertain asset returns; the optimiser splits a budget across
assets. The point of the construction is that some features predict returns
well while others barely predict returns yet decide which assets win the
allocation, so the decision focus has something to recover.
}
\details{
The true return of asset \code{i} in scenario \code{s} is
\verb{mu + sum_pred beta_pred z_sj + sum_dec beta_dec d_sj + noise}. The
prediction-heavy features \code{z_sj} are scenario-level, shared by every asset,
and predict the return strongly. The decision-relevant features \code{d_sj} are
scenario-level draws entering with a small coefficient, so a prediction-tuned
lasso under-selects them while the decision-relevance proxy reads their
scenario-level signal. The remaining features are noise.
}
\examples{
sim <- simulate_capital_allocation(8, 6, 6, seed = 1)
head(sim$data)
attr(sim$data, "decision_relevant_features")

}
\seealso{
\code{\link[=simulate_knapsack]{simulate_knapsack()}}, \code{\link[=simulate_shortest_path]{simulate_shortest_path()}},
\code{\link[=capital_allocation_problem]{capital_allocation_problem()}}

Other dflasso data helpers: 
\code{\link{dfl_data}()},
\code{\link{make_instances}()},
\code{\link{simulate_knapsack}()},
\code{\link{simulate_shortest_path}()}
}
\concept{dflasso data helpers}
