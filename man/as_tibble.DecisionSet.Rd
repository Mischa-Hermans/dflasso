% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/decide.R
\name{as_tibble.DecisionSet}
\alias{as_tibble.DecisionSet}
\title{Tidy a decision set into a long table}
\usage{
\method{as_tibble}{DecisionSet}(x, ...)
}
\arguments{
\item{x}{A \linkS4class{DecisionSet} object.}

\item{...}{Unused, present for method compatibility.}
}
\value{
A tibble with columns \code{scenario}, \code{element_id}, \code{decision}, \code{chosen},
\code{predicted_cost}, \code{contribution}, \code{feasible}, and \code{step}. For an infeasible
instance every element row is kept with \code{decision}, \code{chosen}, and
\code{contribution} set to \code{NA} and \code{predicted_cost} still filled.
}
\description{
Returns one row per (instance, element). \code{element_id} is a real column, so a
\code{left_join} back onto the original rows matches by id, not by row order.
}
\examples{
sim <- simulate_capital_allocation(40, 6, 6, seed = 1)
fit <- dfl_fit(
  sim$x, sim$cost, sim$scenario,
  problem = capital_allocation_problem(max_weight = 0.5),
  element_id = sim$element_id,
  control = dfl_control(seed = 1, n_splits = 5L)
)
picks <- decide(fit, sim$x, sim$scenario, element_id_new = sim$element_id)
head(as_tibble(picks))

}
\seealso{
\code{\link[=decide]{decide()}}, \code{\link[=decisions]{decisions()}}
}
