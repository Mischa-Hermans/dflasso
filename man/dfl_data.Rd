% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/data-helpers.R
\name{dfl_data}
\alias{dfl_data}
\title{Shape one data frame into the inputs for \code{dfl_fit()}}
\usage{
dfl_data(
  data,
  features,
  cost = NULL,
  scenario,
  regret = NULL,
  sense = c("min", "max"),
  element_id = NULL
)
}
\arguments{
\item{data}{A data frame with one row per decision element.}

\item{features}{The feature columns, given as a \pkg{tidyselect}
specification: a character vector, a \code{c(...)} of bare names, or a selector
such as \code{starts_with("feat_")}.}

\item{cost}{The realised-cost column, an unquoted name or a string. \code{NULL}
(default) for the scores-only or supplied-regret path, which needs no
cost.}

\item{scenario}{The grouping column, an unquoted name or a string, saying
which instance each row belongs to.}

\item{regret}{Optional realised-regret column, an unquoted name or a string,
for the supplied-regret path. \code{NULL} on the plain solver path.}

\item{sense}{Objective sense of the diagnosed model, recorded for the
supplied-regret path. \code{"min"} (default) or \code{"max"}.}

\item{element_id}{Optional id column, an unquoted name or a string. \code{NULL}
(default) uses per-scenario row positions.}
}
\value{
A list with \code{x} (a numeric matrix), \code{cost} (a numeric vector or
\code{NULL}), \code{scenario}, \code{regret} (a numeric vector or \code{NULL}), \code{element_id}
(a vector or \code{NULL}), and \code{sense}. Every piece is sliced from the same
rows of \code{data}. Pass it straight to \code{\link[=dfl_fit]{dfl_fit()}}.
}
\description{
The recommended way to feed \code{\link[=dfl_fit]{dfl_fit()}}. Pass one data frame, one row
per decision element, and name the feature, cost, and scenario columns.
\code{dfl_data()} slices every piece from the same rows, so the features, costs
and scenarios stay aligned.
}
\details{
Each row is one decision element in one scenario (for example one asset in
one period), not one row per scenario with its elements spread across
columns. If the table is wide, pivot it long first.

Feature columns are pulled in selection order. Factor or character feature
columns are dummy-expanded with \code{model.matrix(~ . - 1)}, so \code{x} is always
numeric. A text feature with many distinct values (more than 50, or more than
half the rows) is rejected rather than expanded, since that is almost always
an identifier or free-text column selected by mistake; drop or bin it. Costs
and regret may be missing (\code{NA}) but are never imputed.
}
\examples{
sim <- simulate_capital_allocation(8, 6, 6, seed = 1)
prepared <- dfl_data(
  sim$data,
  features = starts_with("feat_"),
  cost = realized_return,
  scenario = scenario,
  element_id = asset_id
)
dim(prepared$x)

}
\seealso{
\code{\link[=make_instances]{make_instances()}}, \code{\link[=dfl_fit]{dfl_fit()}}

Other dflasso data helpers: 
\code{\link{make_instances}()}
}
\concept{dflasso data helpers}
