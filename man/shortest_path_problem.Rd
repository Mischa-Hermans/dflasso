% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/problem-constructors.R
\name{shortest_path_problem}
\alias{shortest_path_problem}
\title{Shortest path through a graph}
\usage{
shortest_path_problem(allow_unreachable = TRUE)
}
\arguments{
\item{allow_unreachable}{Single logical. \code{TRUE} (default) lets a fit carry
instances where the destination cannot be reached after closures, marking
them infeasible rather than failing the batch. \code{FALSE} treats an
unreachable destination as a hard error.}
}
\value{
A \code{ShortestPathProblem}, one of the concrete
\linkS4class{OptimizationProblem} classes, with sense fixed at \code{"min"}. Pass it
to \code{dfl_fit()} as the \code{problem} argument.
}
\description{
A built-in problem for routing decisions: find the least-cost path from an
origin node to a destination node over predicted arc costs. The solver is a
compiled Dijkstra.
}
\examples{
problem <- shortest_path_problem()
problem
instance <- list(
  from = c(1L, 1L, 2L, 3L),
  to = c(2L, 3L, 4L, 4L),
  n_nodes = 4L,
  origin = 1L,
  destination = 4L
)
solve_decision(problem, costs = c(1, 4, 1, 1), instance = instance)

}
\seealso{
\code{\link[=knapsack_problem]{knapsack_problem()}}, \code{\link[=capital_allocation_problem]{capital_allocation_problem()}},
\code{\link[=optimization_problem]{optimization_problem()}}, \code{\link[=solve_decision]{solve_decision()}}

Other dflasso problems: 
\code{\link{OptimizationProblem-class}},
\code{\link{capital_allocation_problem}()},
\code{\link{knapsack_problem}()},
\code{\link{optimization_problem}()},
\code{\link{sense}()},
\code{\link{solve_decision}()}
}
\concept{dflasso problems}
