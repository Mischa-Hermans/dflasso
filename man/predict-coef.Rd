% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/predict-coef.R
\name{predict-coef}
\alias{predict-coef}
\alias{predict.DecisionFocusedLasso}
\alias{coef.DecisionFocusedLasso}
\alias{predict,DecisionFocusedLasso-method}
\alias{coef,DecisionFocusedLasso-method}
\title{Predicted costs and coefficients from a fitted model}
\usage{
\S4method{predict}{DecisionFocusedLasso}(
  object,
  x_new,
  s = "lambda.min",
  penalty = NULL,
  type = c("response", "coefficients", "nonzero"),
  ...
)

\S4method{coef}{DecisionFocusedLasso}(object, s = "lambda.min", penalty = NULL, ...)
}
\arguments{
\item{object}{A \linkS4class{DecisionFocusedLasso} object.}

\item{x_new}{Numeric feature matrix of new rows, columns in the same order
(or with the same names) as the matrix the fit was trained on.}

\item{s}{Penalty strength: \code{"lambda.min"} (default), \code{"lambda.1se"}, or a
numeric lambda.}

\item{penalty}{Which stage to read, \code{NULL} (default) for the fit's primary
stage, or one of \code{"decision"}, \code{"adaptive"}, \code{"plain"} to inspect a
specific stage.}

\item{type}{What to return. \code{"response"} (default) gives the predicted cost
vector, one value per row of \code{x_new}. \code{"coefficients"} gives the sparse
coefficient vector. \code{"nonzero"} gives the names of the kept features.}

\item{...}{Unused, present for S4/S3 compatibility.}
}
\value{
\code{predict()} with \code{type = "response"} returns a numeric vector of
length \code{nrow(x_new)}. With \code{type = "coefficients"} it returns a sparse
coefficient vector including the intercept. With \code{type = "nonzero"} it
returns a character vector of kept feature names. \code{coef()} returns the
sparse coefficient vector including the intercept.
}
\description{
\code{predict()} turns new feature rows into the predicted cost vector the
decision is made from; \code{coef()} returns the sparse coefficient vector.
Neither needs realised costs.
}
\examples{
sim <- simulate_capital_allocation(40, 6, 6, seed = 1)
fit <- dfl_fit(
  sim$x, sim$cost, sim$scenario,
  problem = capital_allocation_problem(max_weight = 0.5),
  element_id = sim$element_id,
  control = dfl_control(seed = 1, n_splits = 5L)
)
head(predict(fit, sim$x))
coef(fit)
predict(fit, sim$x, type = "nonzero")

}
\seealso{
\code{\link[=decide]{decide()}} to act on a fit.

Other dflasso workflow: 
\code{\link{decide}()},
\code{\link{dfl_fit}()},
\code{\link{dfl_score}()},
\code{\link{regret}()}
}
\concept{dflasso workflow}
