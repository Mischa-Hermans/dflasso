% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/summary-print.R
\name{summary.DecisionFocusedLasso}
\alias{summary.DecisionFocusedLasso}
\alias{print.summary.DecisionFocusedLasso}
\title{Summary of a fitted decision-focused lasso}
\usage{
\method{summary}{DecisionFocusedLasso}(object, ...)

\method{print}{summary.DecisionFocusedLasso}(x, ...)
}
\arguments{
\item{object}{A \linkS4class{DecisionFocusedLasso} object.}

\item{...}{Unused, present for method compatibility.}

\item{x}{A \code{summary.DecisionFocusedLasso} object.}
}
\value{
An S3 \code{summary.DecisionFocusedLasso} object carrying the role table,
the rescued feature names, the kept count, coverage and reproducibility
fields, and the resolved settings. Its \code{print()} renders the one-screen
view.
}
\description{
Turns a \linkS4class{DecisionFocusedLasso} fit into a one-screen report: how
many features were kept for the decision, which weak but decision-relevant
features the method rescued, what each feature is for, the filtering strength,
and the reproducibility fields. It reports no held-out regret, because that
needs test data; run \code{\link[=regret]{regret()}} on a held-out split for that.
}
\details{
Each feature is labelled by how it behaves. A feature the accuracy stage (the
ordinary or adaptive lasso) keeps is prediction-relevant. A feature that stage
would drop, yet the decision stage keeps because it tracks decision regret, is
decision-relevant. A feature both stages keep does both; a feature neither
keeps is left out.
}
\examples{
sim <- simulate_capital_allocation(60, 6, 6, seed = 1)
fit <- dfl_fit(
  sim$x, sim$cost, sim$scenario,
  problem = capital_allocation_problem(max_weight = 0.5),
  element_id = sim$element_id,
  control = dfl_control(seed = 1, n_splits = 5L)
)
summary(fit)

}
\seealso{
\code{\link[=dfl_fit]{dfl_fit()}}, \code{\link[=regret]{regret()}} for the held-out comparison, \code{\link[generics:tidy]{generics::tidy()}} for
every feature and its role.
}
