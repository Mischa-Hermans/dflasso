% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/simulate.R
\name{simulate_shortest_path}
\alias{simulate_shortest_path}
\title{Simulate a shortest-path routing problem}
\usage{
simulate_shortest_path(n_days, n_arcs, n_nodes, seed = NULL)
}
\arguments{
\item{n_days}{Number of training days, one instance each.}

\item{n_arcs}{Approximate number of arcs in the graph. The layered
construction may add a few to keep the graph connected.}

\item{n_nodes}{Number of nodes. Origin is pinned to node 1, destination to
node 5.}

\item{seed}{Single integer, or \code{NULL} to draw one at random. The same seed
gives the same data.}
}
\value{
A list with \code{arcs} (arc_id, from_node, to_node, \code{surface},
\code{base_time}, the static loadings \code{flood_load}/\code{mud_load}, and
\code{region_sign}), \code{nodes}, \code{training_days} (date, origin, destination,
\code{closed_arc_ids} list-column), \code{arc_day_features} (date, arc_id, and the
per-arc-per-day feature columns \code{congestion}, \code{rainfall}, \code{flood_depth},
\code{mud_depth}, and \code{noise_01}..\code{noise_24}), \code{observed_times} (date, arc_id,
travel_time, ragged with \code{NA}),
the matching \code{holdout_days}, \code{arc_day_features_holdout},
\code{observed_times_holdout}, and \code{tomorrow_days} (decide-time days including the
infeasible \code{2026-08-14}) with \code{arc_day_features_tomorrow}.
}
\description{
Builds an example single-origin single-destination routing dataset like the
\code{aid_routing} data. Each day is one instance over a connected layered
graph; some days close arcs so the route must change. Features are observed
per arc per day (the \code{arc_day_features} panel) because a router sees that
day's forecast on each arc before driving. Network-wide \code{congestion} raises
every arc's travel time together and predicts it strongly without changing
the route. The two decision-relevant features, \code{flood_depth} and
\code{mud_depth}, are built per arc per day as \verb{weather_day load_arc} with
opposite regional signs, so a wet day tilts north routes against south
routes (changing which route is fastest) while each feature carries only a
small marginal effect on travel time. The prediction-tuned baseline therefore
drops them. Because the day's weather drives both the route tilt and the
feature's per-day mean over the graph, the days where ignoring the feature is
costly are the days its mean is large, which the decision-relevance proxy
detects.
}
\details{
The data is built to exercise the package's edge cases: a fixed subset of
days has full coverage of its solved arcs, a couple of observed rows fall on
closed arcs, and one decide-time day (\code{2026-08-14}) closes every arc out of
the origin, leaving the destination unreachable.
}
\examples{
routing <- simulate_shortest_path(n_days = 12, n_arcs = 20, n_nodes = 8, seed = 1)
names(routing)
head(routing$arc_day_features)

}
\seealso{
\code{\link[=simulate_capital_allocation]{simulate_capital_allocation()}}, \code{\link[=simulate_knapsack]{simulate_knapsack()}},
\code{\link[=shortest_path_problem]{shortest_path_problem()}}

Other dflasso data helpers: 
\code{\link{aid_routing}},
\code{\link{capital_allocation_demo}},
\code{\link{dfl_data}()},
\code{\link{make_instances}()},
\code{\link{prepare_instances}()},
\code{\link{simulate_capital_allocation}()},
\code{\link{simulate_knapsack}()}
}
\concept{dflasso data helpers}
