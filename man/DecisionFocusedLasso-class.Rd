% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/fitted-class.R
\docType{class}
\name{DecisionFocusedLasso-class}
\alias{DecisionFocusedLasso-class}
\alias{show,DecisionFocusedLasso-method}
\title{A fitted decision-focused lasso}
\usage{
\S4method{show}{DecisionFocusedLasso}(object)
}
\description{
The object \code{\link[=dfl_fit]{dfl_fit()}} returns. It holds the three lasso fits (plain,
adaptive, and decision-focused) on one shared set of folds so they compare
directly. It also keeps the per-feature adaptive weights and
decision-relevance scores that shaped the decision-focused fit. The
coverage report and the resolved seed are stored too.
}
\section{Slots}{

\describe{
\item{\code{decision_fit,adaptive_fit,plain_fit}}{The three \code{cv.glmnet} fits. All
three are computed on one shared \code{foldid} so they are comparable; \code{penalty}
chooses which is primary.}

\item{\code{adaptive_weight}}{Numeric vector, one baseline ridge-adaptive weight per
feature.}

\item{\code{proxy_score}}{Numeric vector, the regret-proxy score per feature.}

\item{\code{penalty_factor}}{Numeric vector, the final weights fed to the
decision-focused lasso.}

\item{\code{feature_names}}{Character vector of feature names.}

\item{\code{features_named}}{Single logical, \code{TRUE} when \code{x} carried column names at
fit, which drives new-data matching by name rather than position.}

\item{\code{element_ids}}{List of per-scenario element id vectors.}

\item{\code{coverage}}{Data frame of the per-scenario coverage report, or \code{NULL} for
a supplied-regret fit.}

\item{\code{problem}}{The \linkS4class{OptimizationProblem} object, or \code{NULL} for a
supplied-regret fit.}

\item{\code{sense}}{Character scalar, \code{"min"} or \code{"max"}.}

\item{\code{source}}{Character scalar, \code{"solver"} or \code{"supplied regret"}.}

\item{\code{penalty_primary}}{Character scalar, one of \code{"decision"}, \code{"adaptive"},
\code{"plain"}.}

\item{\code{lambda_min,lambda_1se}}{Numeric scalars, the two standard \code{cv.glmnet}
lambda choices for the primary fit.}

\item{\code{eligibility_threshold}}{Single number, the resolved ridge-weight gate;
\code{0} when no weight gate applies (the default).}

\item{\code{n_proxy_eligible,n_partial_coverage}}{Integer counts of scenarios that
could and could not be scored.}

\item{\code{splits}}{List of the proxy train/validation splits, or \code{NULL} for a
supplied-regret fit.}

\item{\code{seed}}{Single integer, the resolved seed.}

\item{\code{standardize}}{Single logical.}

\item{\code{control}}{The \code{dfl_control} list the fit obeyed.}

\item{\code{call}}{The matched call.}
}}

\seealso{
\code{\link[=dfl_fit]{dfl_fit()}}, \code{\link[=proxy_score]{proxy_score()}}, \code{\link[=penalty_factor]{penalty_factor()}},
\code{\link[=adaptive_weight]{adaptive_weight()}}, \code{\link[=selected_features]{selected_features()}}, \code{\link[=coverage]{coverage()}}, \code{\link[=splits]{splits()}}
}
\keyword{internal}
