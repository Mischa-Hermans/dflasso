% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/data.R
\docType{data}
\name{aid_routing}
\alias{aid_routing}
\title{Aid routing, a single-source shortest-path dataset}
\format{
A list with the fields \code{\link[=simulate_shortest_path]{simulate_shortest_path()}} returns:
\describe{
\item{arcs}{data frame, one row per directed arc: \code{arc_id}, \code{from_node},
\code{to_node}, \code{surface}, \code{base_time}, and the static loadings \code{flood_load},
\code{mud_load}, \code{region_sign}.}
\item{nodes}{data frame: \code{node_id}, \code{layer}, \code{region}. Origin is node 1,
destination is node 5.}
\item{training_days}{data frame, one row per delivery-day: \code{date} (the
scenario id), \code{origin}, \code{destination}, and \code{closed_arc_ids} (a list-column
of arc ids hard-closed that day).}
\item{arc_day_features}{data frame, one row per (date, arc): the feature
columns \code{congestion}, \code{rainfall}, \code{flood_depth}, \code{mud_depth}, and
\code{noise_01} .. \code{noise_24}.}
\item{observed_times}{data frame: \code{date}, \code{arc_id}, \code{travel_time}, a row
only where a time was observed, about half the (date, arc) pairs. A couple
of rows fall on closed arcs, so the coverage report has unplaceable rows.}
\item{holdout_days, arc_day_features_holdout, observed_times_holdout}{the
matching tables for the \code{\link[=regret]{regret()}} held-out days, disjoint from training.}
\item{tomorrow_days, arc_day_features_tomorrow}{the decide-time days (no
costs), including \code{2026-08-14}, which closes a cut-set severing every
origin-to-destination path.}
}
}
\source{
Simulated by \code{simulate_shortest_path(n_days = 250, n_arcs = 30, n_nodes = 12, seed = 7)}; see \code{data-raw/make_datasets.R}.
}
\usage{
aid_routing
}
\description{
A simulated single-source shortest-path (routing) problem on a small road
graph: one origin (node 1), one destination (node 5), per-arc features, and
realised travel times. One vehicle and one origin-to-destination path per day,
solved by Dijkstra, not a vehicle-routing or TSP instance.
}
\details{
A list of graph tables, not element rows. \code{\link[=prepare_instances]{prepare_instances()}} joins them
into the pieces \code{\link[=dfl_fit]{dfl_fit()}}, \code{\link[=decide]{decide()}}, and \code{\link[=regret]{regret()}} consume, one row per
arc per day; each delivery-day is one instance over the day's graph (the arcs
minus that day's closures). \code{flood_depth} and \code{mud_depth} are decision-relevant
but weak: they predict travel time poorly yet flip which route is fastest on
wet days.
}
\examples{
\donttest{
prep <- prepare_instances(aid_routing, which = "training")
fit <- dfl_fit(
  prep$x, prep$cost, prep$scenario,
  problem = shortest_path_problem(),
  instances = prep$instances, element_id = prep$element_id,
  control = dfl_control(seed = 1)
)
fit
}

}
\seealso{
\code{\link[=prepare_instances]{prepare_instances()}}, \code{\link[=shortest_path_problem]{shortest_path_problem()}},
\code{\link[=simulate_shortest_path]{simulate_shortest_path()}}, \code{\link[=dfl_fit]{dfl_fit()}}, \link{capital_allocation_demo}

Other dflasso data helpers: 
\code{\link{capital_allocation_demo}},
\code{\link{dfl_data}()},
\code{\link{make_instances}()},
\code{\link{prepare_instances}()},
\code{\link{simulate_capital_allocation}()},
\code{\link{simulate_knapsack}()},
\code{\link{simulate_shortest_path}()}
}
\concept{dflasso data helpers}
\keyword{datasets}
