% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/simulate.R
\name{simulate_knapsack}
\alias{simulate_knapsack}
\title{Simulate a knapsack problem}
\usage{
simulate_knapsack(n_scenarios, n_items, n_features, seed = NULL)
}
\arguments{
\item{n_scenarios}{Number of scenarios, each one optimisation instance.}

\item{n_items}{Number of items per scenario.}

\item{n_features}{Number of feature columns. The first two are the
decision-relevant features, the next few predict the return, and the rest
are noise. Must be at least three.}

\item{seed}{Single integer, or \code{NULL} to draw one at random. The same seed
gives the same data.}
}
\value{
A list with \code{data} (a data frame: \code{feat_01} .. \verb{feat_<n_features>},
\code{realized_value}, \code{scenario}, \code{item_id}, and \code{weight}), plus \code{x}, \code{cost}
(the realized values), \code{scenario}, \code{weights} (the per-item weights),
\code{capacity} (a default budget of half the total item weight, a binding
limit), and \code{item_id}. Pass the weights and the capacity through
\code{\link[=make_instances]{make_instances()}}.
}
\description{
Builds an example predict-then-optimize dataset for the 0/1 knapsack. Each
scenario is one realisation of
uncertain item values; the optimiser picks items to maximise value under a
single capacity. Item weights are fixed random integers across scenarios.
}
\details{
The true value of item \code{i} in scenario \code{s} follows the same equation as
\code{\link[=simulate_capital_allocation]{simulate_capital_allocation()}}: prediction-heavy scenario-level features
move every item's value together, the two decision-relevant features are
per-item (\verb{g_sj loading_i + small noise}) with a small coefficient, and the
rest are noise.
}
\examples{
sim <- simulate_knapsack(6, 8, 5, seed = 1)
head(sim$data)
instances <- make_instances(
  sim$scenario, weights = sim$weights, capacity = sim$capacity
)

}
\seealso{
\code{\link[=simulate_capital_allocation]{simulate_capital_allocation()}}, \code{\link[=simulate_shortest_path]{simulate_shortest_path()}},
\code{\link[=knapsack_problem]{knapsack_problem()}}

Other dflasso data helpers: 
\code{\link{aid_routing}},
\code{\link{capital_allocation_demo}},
\code{\link{dfl_data}()},
\code{\link{make_instances}()},
\code{\link{prepare_instances}()},
\code{\link{simulate_capital_allocation}()},
\code{\link{simulate_shortest_path}()}
}
\concept{dflasso data helpers}
