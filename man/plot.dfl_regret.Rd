% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/plot.R
\name{plot.dfl_regret}
\alias{plot.dfl_regret}
\alias{autoplot.dfl_regret}
\alias{plot,dfl_regret,missing-method}
\title{Plot the held-out regret against the baseline}
\usage{
\method{autoplot}{dfl_regret}(object, ...)

\method{plot}{dfl_regret}(x, y, ...)

\S4method{plot}{dfl_regret,missing}(x, y, ...)
}
\arguments{
\item{object, x}{A \code{dfl_regret} object, as returned by \code{\link[=regret]{regret()}}.}

\item{...}{Unused, present for method compatibility.}

\item{y}{Not used.}
}
\value{
A ggplot object.
}
\description{
Draws the per-instance regret of the decision-focused fit against the
prediction-focused baseline on the same held-out instances, lower is better,
with the printed average marked. Shows whether the average holds across
instances or rests on a few.
}
\examples{
sim <- simulate_capital_allocation(80, 6, 6, seed = 1)
fit <- dfl_fit(
  sim$x, sim$cost, sim$scenario,
  problem = capital_allocation_problem(max_weight = 0.5),
  element_id = sim$element_id,
  control = dfl_control(seed = 1, n_splits = 5L)
)
result <- regret(fit, sim$x, sim$cost, sim$scenario)
plot(result)

}
\seealso{
\code{\link[=regret]{regret()}}, \link{plot.DecisionFocusedLasso}
}
