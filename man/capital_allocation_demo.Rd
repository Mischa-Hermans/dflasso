% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/data.R
\docType{data}
\name{capital_allocation_demo}
\alias{capital_allocation_demo}
\title{Capital allocation, a budget-split dataset}
\format{
A data frame with 1,350 rows (225 scenarios x 6 assets) and 10
columns:
\describe{
\item{feat_01, feat_02}{numeric. The two decision-relevant features.}
\item{feat_03, feat_04, feat_05, feat_06}{numeric. The remaining four
features.}
\item{realized_return}{numeric. The realised return per asset, the cost
column; needed only to fit.}
\item{scenario}{factor. Which rebalancing this asset belongs to, the
instance id, with levels \code{scenario_01} .. \code{scenario_225}.}
\item{asset_id}{integer. The per-asset label, carried onto decisions.}
\item{split}{character. \code{"train"} for the first 150 scenarios, \code{"test"} for
the held-out 75, so the demo fits on train and checks \code{\link[=regret]{regret()}} on test.}
}
}
\source{
Simulated by \code{simulate_capital_allocation(n_scenarios = 225, n_assets = 6, n_features = 6, seed = 20260601)}, then a \code{split} column added;
see \code{data-raw/make_datasets.R}.
}
\usage{
capital_allocation_demo
}
\description{
A simulated continuous-allocation problem, a portfolio or budget split. One
data frame, one row per (scenario, asset), in the shape \code{\link[=dfl_fit]{dfl_fit()}} takes
directly, so it fits in a single \code{dfl_data} -> \code{dfl_fit} -> \code{decide} pass with
no instances list and no join. Each scenario is one rebalancing: the optimiser
splits a budget across assets to maximise predicted return.
}
\details{
Two of the six features, \code{feat_01} and \code{feat_02}, are decision-relevant but
weak predictors of \code{realized_return}; the rest range from strong to noise.
}
\examples{
problem <- capital_allocation_problem(max_weight = 0.5)
train <- subset(capital_allocation_demo, split == "train")
d <- dfl_data(
  train,
  features = starts_with("feat_"),
  cost = realized_return,
  scenario = scenario,
  element_id = asset_id
)
dim(d$x)

}
\seealso{
\code{\link[=simulate_capital_allocation]{simulate_capital_allocation()}}, \code{\link[=capital_allocation_problem]{capital_allocation_problem()}},
\code{\link[=dfl_data]{dfl_data()}}, \link{aid_routing}

Other dflasso data helpers: 
\code{\link{aid_routing}},
\code{\link{dfl_data}()},
\code{\link{make_instances}()},
\code{\link{prepare_instances}()},
\code{\link{simulate_capital_allocation}()},
\code{\link{simulate_knapsack}()},
\code{\link{simulate_shortest_path}()}
}
\concept{dflasso data helpers}
\keyword{datasets}
