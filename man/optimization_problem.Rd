% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/problem-constructors.R
\name{optimization_problem}
\alias{optimization_problem}
\title{Wrap a custom solver as a dflasso problem}
\usage{
optimization_problem(
  solve,
  sense = c("min", "max"),
  solve_support = NULL,
  name = NULL
)
}
\arguments{
\item{solve}{A function of \verb{(costs, instance)} returning the decision
vector for that instance. Its length and row order match \code{costs}.}

\item{sense}{Objective direction for \verb{cost' decision}, \code{"min"} (default) or
\code{"max"}.}

\item{solve_support}{Optional function of one argument (the instance)
returning the integer row indices the solver could use in a feasible
decision. This sets the coverage rule: a scenario is eligible for the
decision-quality step only when every row in its support has an observed
cost. \code{NULL} (default) assumes every row of an instance may be used,
which on partially observed data can make scenarios ineligible. Supply one
when the solver can never use some rows (so those rows being unobserved
should not disqualify the scenario), most often when only a subset of
elements can ever enter a feasible decision.}

\item{name}{Optional character label shown by \code{show()}. \code{NULL} (default)
uses \code{"custom"}.}
}
\value{
A \code{FunctionProblem}, one of the concrete
\linkS4class{OptimizationProblem} classes. Pass it to \code{dfl_fit()} as the
\code{problem} argument.
}
\description{
Builds a problem from a custom solver, for the case where no built-in fits.
Supply one function that turns a vector of predicted costs into a decision,
plus the objective sense, and dflasso treats it like any other problem.
}
\examples{
pick_cheapest_half <- function(costs, instance) {
  as.numeric(costs <= stats::median(costs))
}
problem <- optimization_problem(solve = pick_cheapest_half, sense = "min")
problem
solve_decision(problem, c(3, 1, 4, 2), instance = list())

}
\seealso{
\code{\link[=knapsack_problem]{knapsack_problem()}}, \code{\link[=shortest_path_problem]{shortest_path_problem()}},
\code{\link[=capital_allocation_problem]{capital_allocation_problem()}}, \code{\link[=solve_decision]{solve_decision()}}

Other dflasso problems: 
\code{\link{OptimizationProblem-class}},
\code{\link{capital_allocation_problem}()},
\code{\link{knapsack_problem}()},
\code{\link{sense}()},
\code{\link{shortest_path_problem}()},
\code{\link{solve_decision}()}
}
\concept{dflasso problems}
