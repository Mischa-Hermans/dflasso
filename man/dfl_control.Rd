% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/control.R
\name{dfl_control}
\alias{dfl_control}
\title{Options for a dflasso fit}
\usage{
dfl_control(
  seed = NULL,
  parallel = FALSE,
  workers = NULL,
  progress = interactive(),
  standardize = TRUE,
  min_elements_per_scenario = 2L,
  nfolds = 10L,
  gamma = 2,
  kappa = 6,
  proxy_score_reference = 0.2,
  w_min = 0.1,
  w_max = 500,
  n_splits = 15L,
  split_fraction = 0.7,
  eligibility_threshold = NULL,
  score_floor = 0.001,
  ...
)
}
\arguments{
\item{seed}{Single integer, or \code{NULL} (default) to draw one at random. The
resolved seed is stored on the fit.}

\item{parallel}{Single logical, default \code{FALSE}. Run the proxy loop in
parallel.}

\item{workers}{Single positive integer, or \code{NULL} (default). The number of
parallel workers: set it to start a cluster of that size and run in
parallel; \code{NULL} leaves parallelism to \code{parallel}.}

\item{progress}{Single logical, default \code{\link[=interactive]{interactive()}}. When \code{TRUE}, the
decision-quality step prints a one-line start and a \code{Done.} when it
finishes; when \code{FALSE}, that step is silent.}

\item{standardize}{Single logical, default \code{TRUE}, passed to glmnet's
\code{standardize}, which scales features for the fit and returns coefficients
on the original scale.}

\item{min_elements_per_scenario}{Single positive integer, default \code{2}. Drop
scenarios smaller than this after missing costs are removed.}

\item{nfolds}{Single integer of at least 3 (glmnet's minimum), default \code{10}.
Cross-validation folds for the final weighted lasso.}

\item{gamma}{Single positive number, default \code{2}. The adaptive-weight
exponent in \code{1 / abs(beta_ridge)^gamma}.}

\item{kappa}{Single positive number, default \code{6}, the decision-relevance
strength. A rescued feature's penalty is
\code{w_max * exp(-kappa * rescaled_score)}, so a higher \code{kappa} discounts a
high-scoring feature harder.}

\item{proxy_score_reference}{Single positive number, default \code{0.2}, typically
\verb{(0, 1]}. The reference the proxy score is rescaled against; must be
positive because it divides the score.}

\item{w_min}{Single positive number below \code{w_max}, default \code{0.1}. The lower
bound on the final weights.}

\item{w_max}{Single positive number above \code{w_min}, default \code{500}. The upper
bound on the final weights, and where the rescue starts: a decision-relevant
feature is pulled down from \code{w_max} by its score, so it reaches a selectable
penalty whatever its raw adaptive weight.}

\item{n_splits}{Single integer of at least 2, default \code{15}. The
train/validation resamples the proxy averages over.}

\item{split_fraction}{Single number in \verb{(0, 1)}, default \code{0.7}. The training
share within each proxy split.}

\item{eligibility_threshold}{\code{NULL} (default) or a single positive number, a
minimum adaptive weight for rescue. \code{NULL} applies no such gate: a feature
is rescued on its decision-relevance score alone, whatever its adaptive
weight. A positive number (such as one from \link{dflasso_tuned_thresholds})
restricts the rescue to features whose adaptive weight is at least that.}

\item{score_floor}{Single positive number, default \code{1e-3}. Raw proxy scores
below this trigger no discount.}

\item{...}{Caught only to reject unknown arguments with a did-you-mean
suggestion.}
}
\value{
A validated list of class \code{dfl_control}, ready to pass to the
\code{control} argument of \code{\link[=dfl_fit]{dfl_fit()}}.
}
\description{
Collects every setting a \code{\link[=dfl_fit]{dfl_fit()}} run uses into one validated object.
The arguments fall into two groups. The main settings are a seed, whether to
run in parallel, whether to print progress, and the cross-validation and
coverage controls. The method settings control how the decision-relevance
score rescales each feature's penalty; their defaults are tuned.
}
\details{
\code{dfl_control()} validates everything up front, so a bad value or a mistyped
name fails at the call rather than inside a fit.
An unknown argument is rejected with a did-you-mean suggestion.
}
\examples{
dfl_control()
dfl_control(seed = 2024, workers = 8)
dfl_control(eligibility_threshold = dflasso_tuned_thresholds[["knapsack"]])

}
\seealso{
\code{\link[=dfl_fit]{dfl_fit()}}, \link{dflasso_tuned_thresholds}
}
