% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/decide.R
\name{decide}
\alias{decide}
\alias{decide,DecisionFocusedLasso-method}
\title{Make decisions on new data}
\usage{
decide(
  object,
  x_new,
  scenario_new,
  instances_new = NULL,
  element_id_new = NULL,
  problem = NULL,
  s = "lambda.min",
  ...
)

\S4method{decide}{DecisionFocusedLasso}(
  object,
  x_new,
  scenario_new,
  instances_new = NULL,
  element_id_new = NULL,
  problem = NULL,
  s = "lambda.min",
  ...
)
}
\arguments{
\item{object}{A \linkS4class{DecisionFocusedLasso} object.}

\item{x_new}{Numeric feature matrix of new rows, columns in the same order
(or with the same names) as the matrix the fit was trained on.}

\item{scenario_new}{Vector grouping the new rows into instances, one value
per row.}

\item{instances_new}{Optional named list of per-instance data, one entry per
scenario. \code{NULL} (default) auto-builds it from \code{scenario_new} exactly as
\code{\link[=dfl_fit]{dfl_fit()}} does.}

\item{element_id_new}{Optional per-row element ids, used to name every
decision. \code{NULL} (default) uses per-scenario row positions.}

\item{problem}{Optional \linkS4class{OptimizationProblem} to attach to a fit made
from supplied regret. \code{NULL} (default) uses the fit's own solver and errors
if there is none.}

\item{s}{Penalty strength for the predicted costs: \code{"lambda.min"} (default),
\code{"lambda.1se"}, or a numeric lambda.}

\item{...}{Unused, present for S4 compatibility.}
}
\value{
A \linkS4class{DecisionSet} object, one record per instance. Read it with
\code{decisions()}, \code{selected_elements()}, \code{objectives()}, \code{is_feasible()}, and
\code{element_sequence()}; subset it with \code{feasible()} / \code{infeasible()}; list the
reasons with \code{infeasible_reasons()}; and pull a long, join-ready table with
\code{as_tibble()}.
}
\description{
Given new feature rows, \code{decide()} predicts costs and solves each instance
to a decision, returning a \linkS4class{DecisionSet} to act on. It needs no
realised costs.
}
\details{
One infeasible instance never aborts the batch: a scenario the solver cannot
satisfy comes back with \code{feasible = FALSE} and a reason, while the others
still return their decisions.

A fit made from supplied regret has no solver, so \code{decide()} errors unless a
solver is attached with \verb{problem =} (no re-fit needed). Passing \verb{problem =}
to a fit that already has one also errors.

Each record carries the per-instance \code{decision} (named by \code{element_id}: \code{0}/\code{1}
for selection and routing, the weight for allocation), \code{selected_elements}
(the chosen ids), \code{element_sequence} (the ordered ids for graph problems, else
\code{NULL}), \code{predicted_objective} (\verb{cost_hat' decision}), \code{feasible}, and a
\code{message} reason when infeasible. An infeasible instance carries a typed empty
decision (\code{numeric(0)}), not a bare \code{NULL}, so downstream code spots it with
\code{length(decision) == 0L}.
}
\examples{
sim <- simulate_capital_allocation(40, 6, 6, seed = 1)
fit <- dfl_fit(
  sim$x, sim$cost, sim$scenario,
  problem = capital_allocation_problem(max_weight = 0.5),
  element_id = sim$element_id,
  control = dfl_control(seed = 1, n_splits = 5L)
)
picks <- decide(fit, sim$x, sim$scenario, element_id_new = sim$element_id)
picks
head(as_tibble(picks))

}
\seealso{
\code{\link[=dfl_fit]{dfl_fit()}}, \code{\link[=regret]{regret()}} to trust-check the decisions,
\linkS4class{DecisionSet}.

Other dflasso workflow: 
\code{\link{dfl_fit}()},
\code{\link{dfl_score}()},
\code{\link{predict-coef}},
\code{\link{regret}()}
}
\concept{dflasso workflow}
