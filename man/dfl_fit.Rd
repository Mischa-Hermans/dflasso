% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/fit.R
\name{dfl_fit}
\alias{dfl_fit}
\title{Fit a decision-focused lasso}
\usage{
dfl_fit(
  data_or_x,
  cost,
  scenario,
  problem = NULL,
  regret = NULL,
  sense = c("min", "max"),
  instances = NULL,
  element_id = NULL,
  penalty = c("decision", "adaptive", "plain"),
  control = dfl_control(),
  ...
)
}
\arguments{
\item{data_or_x}{The numeric feature matrix \code{x}, one row per element. Build
it from a data frame with \code{\link[=dfl_data]{dfl_data()}} first, then pass \code{prepared$x}; a
data frame is not read here (unlike \code{\link[=dfl_score]{dfl_score()}}, which takes a
\verb{features =} spec).}

\item{cost}{Numeric vector of realised costs, one per row, \code{NA} allowed.
Required on both paths to fit the cost model.}

\item{scenario}{Vector grouping rows into instances; each distinct value is
one instance.}

\item{problem}{An \linkS4class{OptimizationProblem} object for the solver path.
Supply this or \code{regret}, not both.}

\item{regret}{Named per-scenario regret for the supplied-regret path.
Supply this or \code{problem}, not both.}

\item{sense}{Used only with \verb{regret =}, to record and echo the diagnosed
objective's direction. The solver path reads sense off \code{problem}.}

\item{instances}{Optional named list of per-scenario instances. \code{NULL}
auto-builds it from \code{scenario} for built-ins that derive their instance
from element counts and for custom solvers needing no per-instance data.}

\item{element_id}{Optional per-row element ids. \code{NULL} uses per-scenario row
positions.}

\item{penalty}{Which stage is primary for bare \code{coef}/\code{predict}/\code{decide},
one of \code{"decision"} (default), \code{"adaptive"}, \code{"plain"}. All three are
always computed.}

\item{control}{A \code{dfl_control} list of settings.}

\item{...}{Caught only to give a clear error. \code{dfl_fit} takes the numeric
\code{x}/\code{cost}/\code{scenario} that \code{\link[=dfl_data]{dfl_data()}} returns, not a \verb{features =}
tidyselect spec like \code{\link[=dfl_score]{dfl_score()}}; an unmatched argument here is reported
rather than silently dropped.}
}
\value{
An S4 \linkS4class{DecisionFocusedLasso} object.
}
\description{
Learns a sparse linear cost model whose feature selection is driven by
downstream decision quality, not prediction error alone. It fits a plain
lasso and an adaptive lasso alongside the decision-focused fit, all on one
shared fold structure.
}
\details{
One row per decision element per scenario (for example one row per asset per
period), not one row per scenario. \code{\link[=dfl_data]{dfl_data()}} shapes it from one frame;
\code{cost} and \code{scenario} carry one value per row, aligned to the rows of \code{x}.

Two entry modes share this one function. The \strong{solver path} (\verb{problem =})
computes regret by solving each scenario under predicted and realised costs,
scores how strongly each feature tracks that regret, and eases the penalty on
the features that move the decision. The \strong{supplied-regret path} (\verb{regret =})
skips the solver entirely and reads the per-scenario regret supplied; the rest
of the fit is the same. Supply exactly one of \code{problem} or \code{regret}.
}
\examples{
sim <- simulate_capital_allocation(40, 6, 6, seed = 1)
fit <- dfl_fit(
  sim$x, sim$cost, sim$scenario,
  problem = capital_allocation_problem(max_weight = 0.5),
  element_id = sim$element_id,
  control = dfl_control(seed = 1, n_splits = 5L)
)
proxy_score(fit)

}
\seealso{
\code{\link[=dfl_control]{dfl_control()}}, \code{\link[=dfl_data]{dfl_data()}}, \code{\link[=proxy_score]{proxy_score()}},
\linkS4class{DecisionFocusedLasso}

Other dflasso workflow: 
\code{\link{decide}()},
\code{\link{dfl_score}()},
\code{\link{predict-coef}},
\code{\link{regret}()}
}
\concept{dflasso workflow}
