% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/dfl-score.R
\name{dfl_score}
\alias{dfl_score}
\alias{print.dfl_score}
\title{Rank features by how strongly they track regret}
\usage{
dfl_score(
  data_or_x,
  features,
  scenario,
  regret,
  sense = c("min", "max"),
  control = dfl_control()
)

\method{print}{dfl_score}(x, ...)
}
\arguments{
\item{data_or_x}{One data frame read like \code{\link[=dfl_data]{dfl_data()}} (with \code{features} a
tidyselect spec and \code{scenario}/\code{regret} column names), or a numeric feature
matrix \code{x} with loose \code{scenario}/\code{regret} vectors. In the matrix form
\code{regret} must self-key: a numeric named by scenario id, or a two-column
\verb{(id, regret)} data frame. A bare positional vector is rejected.}

\item{features}{The feature columns when \code{data_or_x} is a data frame, a
tidyselect specification.}

\item{scenario}{The grouping column (data-frame form) or per-row scenario
vector (matrix form).}

\item{regret}{The realised-regret column (data-frame form) or the self-keyed
regret (matrix form). One value per scenario.}

\item{sense}{Objective sense of the diagnosed model, \code{"min"} (default) or
\code{"max"}. Recorded, not used to transform supplied regret.}

\item{control}{A \code{dfl_control} list. Only \code{score_floor} is read here.}

\item{x}{A \code{dfl_score} object.}

\item{...}{Unused, present for method compatibility.}
}
\value{
An S3 \code{dfl_score} object: a ranked tibble with columns \code{rank}, \code{term},
\code{proxy_score}, \code{role} (an ordered factor, \code{decision-relevant} or
\code{neither}), and \code{reading} (a plain gloss of the score band). Its \code{print()}
shows the ranking and the not-validated footer.
}
\description{
When per-instance regret is already available, \code{dfl_score()} ranks features
by how strongly each correlates with that regret, skipping the model fit and
solver that \code{\link[=dfl_fit]{dfl_fit()}} runs.
}
\examples{
set.seed(1)
backtest <- data.frame(
  date = rep(c("d1", "d2", "d3", "d4", "d5", "d6"), each = 4),
  feat_rain = rnorm(24), feat_speed = rnorm(24), feat_hist = rnorm(24),
  regret = rep(c(4.1, 0.0, 7.8, 1.2, 6.5, 0.3), each = 4)
)
dfl_score(
  backtest,
  features = starts_with("feat_"), scenario = date, regret = regret
)

}
\seealso{
\code{\link[=dfl_fit]{dfl_fit()}} to also fit a model, \code{\link[=regret_from_objectives]{regret_from_objectives()}} and
\code{\link[=regret_from_decisions]{regret_from_decisions()}} to build the regret vector first.

Other dflasso workflow: 
\code{\link{decide}()},
\code{\link{dfl_fit}()},
\code{\link{predict-coef}},
\code{\link{regret}()}
}
\concept{dflasso workflow}
