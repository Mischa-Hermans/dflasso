% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/plot.R
\name{plot.DecisionFocusedLasso}
\alias{plot.DecisionFocusedLasso}
\alias{autoplot.DecisionFocusedLasso}
\alias{plot,DecisionFocusedLasso,missing-method}
\title{Plot a fitted decision-focused lasso}
\usage{
\method{autoplot}{DecisionFocusedLasso}(object, type = c("roles", "penalty", "path"), ...)

\S4method{plot}{DecisionFocusedLasso,missing}(x, type = c("roles", "penalty", "path"), ...)
}
\arguments{
\item{object, x}{A \linkS4class{DecisionFocusedLasso} object.}

\item{type}{Which view to draw: \code{"roles"} (default), \code{"penalty"}, or
\code{"path"}.}

\item{...}{Unused, present for method compatibility.}
}
\value{
A ggplot object.
}
\description{
Three views of a \linkS4class{DecisionFocusedLasso} fit, each a ggplot. The default
\code{"roles"} view shows which features look weak at predicting cost yet move the
decision, so the method kept them. \code{"penalty"} shows how much the decision
focus eased the penalty on those features; \code{"path"} shows the coefficient
path of the decision-focused fit.
}
\details{
Feature labels use \code{ggrepel} when it is installed, and ordinary text labels
otherwise.
}
\examples{
sim <- simulate_capital_allocation(60, 6, 6, seed = 1)
fit <- dfl_fit(
  sim$x, sim$cost, sim$scenario,
  problem = capital_allocation_problem(max_weight = 0.5),
  element_id = sim$element_id,
  control = dfl_control(seed = 1, n_splits = 5L)
)
plot(fit)
plot(fit, type = "penalty")

}
\seealso{
\code{\link[=dfl_fit]{dfl_fit()}}, \code{\link[=regret]{regret()}} and its \code{plot()} for the held-out spread.
}
